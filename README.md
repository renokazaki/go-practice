# go-practice

## 2. Go言語・基礎

## 7. module・package 4:09

パッケージ内では小文字で変数や関数が共有可能

別パッケージでの参照は大文字で参照が可能

## 2. Go言語・基礎

## 8. variables 0:26

var は型を記載して、初期値の省略が可能。祖場合０値が設定される
また、ポインタ変数も作成され、そこの値の初期値が格納される

一方:の方は初期値を明示的に代する必要がある。型は推定してくれる

この二つの違いは関数の内外で使用が可能かで、前者のvarはjsvascriptと同じ感じで関数外でも可能で、後者は関数内のみでの使用になる

## 2. Go言語・基礎

## 8. variables 5:32

%v ⇒value

%t⇒型

## 2. Go言語・基礎

## 9. pointer・shadowing 1:09

&変数名でメモリのアドレスを取得できる。

この値へ可変で、タイミングや環境で異なる

## 2. Go言語・基礎

## 9. pointer・shadowing \*変数名はポインタ変数。必ず8byte 3:09

ポインタ変数は、ある変数の先頭のアドレス

## 2. Go言語・基礎

## 9. pointer・shadowing 12:28

:=での再定義は別のメモリ領域に作成されるため、同じ値ではない
